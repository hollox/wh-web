export interface Ticket {
  ticketId: string;
  authorUserId: string;
  title: string;
  content: string;
}

export interface TicketJson {
  ticket_id: string;
  author_user_id: string;
  title: string;
  content: string;
}

